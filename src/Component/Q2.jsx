export default function Q2({ backgroundColor, color, borderRadius, padding }) {
  return (
    <div>
      <h2>Question 2</h2>
      <button style={{ color, backgroundColor, borderRadius, padding }}>
        Start Button
      </button>
    </div>
  );
}
